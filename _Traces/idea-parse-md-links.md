idea: parse .md for links, and even if Kelp isn't currently showing the destinations, hint them somehow, so it's quick and easy to navigate around. This would allow Links.md file in for example a folder with videos, that points to a bunch of other folders. This is essentially generalizing symlinks - no longer just filesystem, they can point at concepts ([[wikilinks]] really are pointing at concepts, not exact pages! but REALLY they are pointing AT TAGS), at anything that is addressable. Location, concept (location is just a concept-with-a-path), web...