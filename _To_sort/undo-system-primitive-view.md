## Undo as System Primitive

If a view shows a file that was changed by an AI agent... what does undo mean? Does it revert the file to its previous state? **Is undo a system-level primitive now?** This is a conceptual problem that needs an answer before the AI integration lands.